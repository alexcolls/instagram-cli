# Instagram CLI Configuration
# Copy this file to .env and fill in your settings

# Session Storage
# ---------------
# Path where Instagram session will be saved (default: ~/.instagram_session.json)
# The session file stores your authentication state so you don't have to login every time
INSTAGRAM_SESSION_FILE=~/.instagram_session.json

# Usage Instructions
# ------------------
# 1. Copy this file: cp .env.sample .env
# 2. The default session file location should work for most users
# 3. Run 'instagram-cli login' to authenticate
# 4. Your credentials are never stored - only the session token
